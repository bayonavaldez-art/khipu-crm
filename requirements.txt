# Dependencias del proyecto — Khipu CRM Académico-Comercial
# Instalar con: pip install -r requirements.txt

# API / Backend
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
PyJWT>=2.8
pydantic[email]>=2.6
# Driver PostgreSQL (despliegue en Render/Docker; local usa SQLite)
psycopg[binary]>=3.1

# Inteligencia Artificial (scikit-learn, pandas, numpy)
scikit-learn>=1.4
pandas>=2.2
numpy>=1.26
joblib>=1.3

# Pruebas
pytest>=8.0
httpx>=0.27
